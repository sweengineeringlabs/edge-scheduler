//! Interface counterpart to `core/scheduler/tokio_scheduler.rs`.
//!
//! The tokio scheduler implementation lives in `core/`; its contract is the
//! [`Scheduler`] trait defined in `api/scheduler/scheduler.rs`.
