//! [`ApplicationConfigBuilder`] — entry point for assembling application config.

/// Assembles application-level configuration from layered TOML sources.
#[allow(dead_code)]
pub struct ApplicationConfigBuilder;
